<resources>

    <!-- Base application theme. -->
    <!-- light theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primaryDark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="backgroundColor">@color/background</item>
        <item name="cardBackground">@color/cardColor</item>
        <item name="textColor">@color/textColor</item>
        <item name="mainTextColor">@color/textColor</item>
        <item name="tabTextColor">@color/textColorDark</item>
        <item name="tintColor">@color/tintColor</item>
        <item name="android:windowAnimationStyle">@style/WindowAnimationTransition</item>
    </style>

    <!-- dark theme. -->
    <style name="AppThemeDark" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/primaryNight</item>
        <item name="colorPrimaryDark">@color/primaryDarkNight</item>
        <item name="colorAccent">@color/accent</item>
        <item name="backgroundColor">@color/backgroundDark</item>
        <item name="cardBackground">@color/cardColorDark</item>
        <item name="textColor">@color/textColorDark</item>
        <item name="mainTextColor">@color/textColorDark</item>
        <item name="tabTextColor">@color/accent</item>
        <item name="tintColor">@color/tintColorDark</item>
        <item name="android:windowAnimationStyle">@style/WindowAnimationTransition</item>
    </style>

    <!-- light no action bar theme. -->
    <style name="AppThemeNoAction" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primaryDark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="backgroundColor">@color/background</item>
        <item name="cardBackground">@color/cardColor</item>
        <item name="textColor">@color/textColor</item>
        <item name="mainTextColor">@color/textColor</item>
        <item name="tabTextColor">@color/textColorDark</item>
        <item name="tintColor">@color/tintColor</item>
        <item name="android:windowAnimationStyle">@style/WindowAnimationTransition</item>
    </style>

    <!-- dark no action bar theme. -->
    <style name="AppThemeNoActionDark" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/primaryNight</item>
        <item name="colorPrimaryDark">@color/primaryDarkNight</item>
        <item name="colorAccent">@color/accent</item>
        <item name="backgroundColor">@color/backgroundDark</item>
        <item name="cardBackground">@color/cardColorDark</item>
        <item name="textColor">@color/textColorDark</item>
        <item name="mainTextColor">@color/textColorDark</item>
        <item name="tabTextColor">@color/accent</item>
        <item name="tintColor">@color/tintColorDark</item>
        <item name="android:windowAnimationStyle">@style/WindowAnimationTransition</item>
    </style>

    <style name="WindowAnimationTransition">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>

</resources>
